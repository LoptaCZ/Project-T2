package com.raven_cze.projt2.common.content.tiles;

import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.entity.BlockEntity;
import net.minecraft.world.level.block.state.BlockState;

public class TileDarknessGenerator extends BlockEntity {
    public TileDarknessGenerator(BlockPos pWorldPosition, BlockState pBlockState) {
        super(null,pWorldPosition,pBlockState);
    }
}
